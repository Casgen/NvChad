local options = {
  ensure_installed = { "lua" },

  highlight = {
    enable = true,
    use_languagetree = true,
    ignore_install = { 'latex' }
  },

  indent = { enable = true },
}

return options
